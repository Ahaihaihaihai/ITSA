#include <iostream>
#include <iomanip>
using namespace std;

int main(void)
{
	int B, T, H;
    cin >> T >> B >> H;
    cout << fixed << setprecision(1) << "Trapezoid area:" << (double)(T+B)*H/2 << endl;
	
	
	return 0;
}
